name: test

on:
  push:
    tags: ""
    branches: main
    paths-ignore:
      - "**/*.md"
  pull_request:
    branches: main
    paths-ignore:
      - "**/*.md"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: test
        run: | 
          pwd &&
          ls -la &&
          which docker &&
          docker --version &&
          echo ${REGISTRY} &&
          echo ${github.actor} &&
          echo ${github.run_number} &&
          echo ${GITHUB_WORKSPACE} 

   